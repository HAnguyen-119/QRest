import { StyleSheet } from "react-native"

export const OrderListStyles = StyleSheet.create({
    container: {
        padding: 12,
        borderWidth: 2,
        borderRadius: 20,
        margin: 12,
    }
})