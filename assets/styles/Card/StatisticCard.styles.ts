import { StyleSheet } from "react-native"

export const createStatisticCardStyles = (isDark: boolean) => {
    return StyleSheet.create({
        container: {

        },
        title: {

        },
    })
}